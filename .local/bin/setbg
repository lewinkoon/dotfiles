#!/usr/bin/env sh

#[ ! -z "$1" ] && convert "$1" ~/.config/wall/wall.png &&
#convert "$1" -crop 50%x1080 ~/.config/wall/wall.png &&
xwallpaper --output DP-0 --zoom $1
xwallpaper --output DP-2 --zoom $1
notify-send -i "$1" "Wallpaper changed."
