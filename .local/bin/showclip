#!/bin/sh

# display contents of selection via dunst if running

clip=$(xclip -o -selection clipboard)
prim=$(xclip -o -selection primary)

[ -n "$clip" ] && notify-send "Clipboard" "\n### MAIN ###\n$clip\n\n### PRIMARY ###\n$prim"
